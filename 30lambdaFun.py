# a lambda function is a small, anonymous function defined using the lambda keyword.
# Lambda functions are also known as anonymous functions or lambda expressions.
# They are typically used for short, simple operations where
# defining a full function using the def keyword would be unnecessary.

lambda num : num * 2

multiply = lambda a, b : a * b

print(multiply(2,4))